{{-- Moderation state, so it sits alongside the lifecycle badge rather than replacing it. --}}
<span {{ $attributes->merge(['class' => 'inline-flex items-center rounded-full bg-soil-200 px-2.5 py-0.5 font-body text-xs font-semibold text-soil-700']) }}>
    Hidden
</span>
